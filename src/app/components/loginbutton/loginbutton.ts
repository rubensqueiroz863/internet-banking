import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  selector: 'app-loginbutton',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './loginbutton.html',
  styleUrl: './loginbutton.scss',
})
export class Loginbutton {

}
